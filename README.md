# Introduction

Simple Reactive Excel that updates value for you automatically

# Setup

1. Clone this repository
2. Run `bun install` to install dependencies
3. Run `bun run dev` to start local development
